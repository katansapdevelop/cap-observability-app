{
  "name": "cap-template-app",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^7",
    "express": "^4",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1",
    "@sap/cds-dk": "^7.4.0"
  },
  "scripts": {
    "start": "cds watch"
  },
  "cds": {
    "requires": {
      "metrics": {
        "kind": "odata",
        "model": "srv/metrics-service",
        "service": "logicalstar.srv.BookMetricsService"
      }
    }
  }
}
